{
  "name": "azure-function-squad-sample",
  "version": "1.0.0",
  "private": true,
  "description": "Azure Function + Content Review Squad — Squad SDK sample",
  "type": "module",
  "scripts": {
    "start": "npx tsx src/functions/squad-prompt.ts",
    "start:func": "func start",
    "build": "tsc",
    "build:check": "tsc --noEmit",
    "test": "npx tsx src/functions/squad-prompt.ts --dry-run"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@bradygaster/squad-sdk": "file:../../packages/squad-sdk"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
